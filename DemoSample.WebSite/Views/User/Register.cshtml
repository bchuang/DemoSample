@{
    ViewBag.Title = "Register";
}

<!-- Page Header -->
<div class="container">
    <header class="page-header">
        <div class="branding">
            <img src="https://vuejs.org/images/logo.png" alt="Logo" title="Home page" class="logo" />
            <h1>Vue.js CRUD application</h1>
        </div>
    </header>
    <main id="app">
        <h2>Add new user</h2>
        <form>
            <div class="form-group">
                <label for="add-acct">Acct</label>
                <input class="form-control" id="add-acct" v-model="user.acct" required />
                <div style="color:red" v-if="validated.acct.length>0">
                    驗證:
                    <span v-for="item in validated.acct">
                        {{item}}
                    </span>
                </div>
            </div>
            <div class="form-group">
                <label for="add-name">Name</label>
                <input class="form-control" id="add-name" v-model="user.name" required />
                <div style="color:red" v-if="validated.name.length>0">
                    驗證:
                    <span v-for="item in validated.name">
                        {{item}}
                    </span>
                </div>
            </div>
            <button type="button" class="btn btn-primary" v-on:click="create_user">Create</button>
            <a href="/demo" class="btn btn-default">Cancel</a>
        </form>
    </main>
</div>
<script src="~/Scripts/Modules/User/Base.js"></script>
<script src="~/Scripts/Modules/User/Register.js"></script>

<!-- Template Create -->
@*<template id="add-user">
        <h2>Add new user</h2>
        <form>
            <div class="form-group">
                <label for="add-name">Name</label>
                <input class="form-control" id="add-name" v-model="user.name" required />
            </div>
            <div class="form-group">
                <label for="add-acct">acct</label>
                <textarea class="form-control" id="add-acct" rows="10" v-model="user.acct"></textarea>
            </div>
            <div class="form-group">
                <label for="add-memo">memo</label>
                <input type="number" class="form-control" id="add-memo" v-model="user.memo" />
            </div>
            <button type="button" class="btn btn-primary">Create</button>
            <a href="/demo" class="btn btn-default">Cancel</a>
        </form>
    </template>*@